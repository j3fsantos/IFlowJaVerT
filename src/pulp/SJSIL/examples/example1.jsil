proc noUnreachableWithErrorToErrToRet (x) {
			goto [x] here there;
			[[ xret == 0 ]]
	here:	xret := 1
	        [[ xret == 1 ]];
			goto rlab;
	there:	xret := 2;
	rlab:	skip
}
with
{
	ret: xret, rlab;
}