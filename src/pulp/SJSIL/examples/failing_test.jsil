pred p1 ("hello", x) : x == "hi",
                       false;

spec xpto (x)
   [[ ((x - 3) <=# 0) * types(x: $$int_type) ]]
   [[ p1("hello", "hi") ]]
   normal
proc xpto (x) {
			skip;
			[* fold p1("hello", "hi") *]
	rlab:   skip
}
with
{
	ret: 	x, rlab;
};
