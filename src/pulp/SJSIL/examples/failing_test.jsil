(** Tested feature: property check 1                 **)
(** Expected result: YES                             **)
spec test3 (x)
   [[ ((x, "cenoura") -> #y) * ((x, "@proto") -> $$null) * (! #y == None) * types(#y: $$string_type) ]]
   [[ ((x, "cenoura") -> #y) * ((x, "@proto") -> $$null) * types(#y: $$string_type) * (ret == $$t) ]]
   normal
proc test3 (x) {
  		   y := hasField(x, "cenoura");
   rlab:   skip
} with { ret: y, rlab; };
