spec xpto (x)
   [[ ((x - 3) <<= 0) * types(x: $$int_type)]]
   [[ ((x + 1) <<= 0) ]]
   normal
proc xpto (x) {
               skip;
       rlab:   skip;
       elab:   skip
}
with
{
      ret: 	x, rlab;
      err: 	x, elab;
};


(** Check if (x == "maria") entails (x == "maria") **)
(** Expected result: YES                           **)
spec test1 (x)
   [[ (x == "maria") ]]
   [[ (x == "maria") ]]
   normal
proc test1 (x) {
   rlab:   skip
} with { ret: x, rlab; };
