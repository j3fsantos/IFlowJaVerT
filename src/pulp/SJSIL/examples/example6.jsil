
spec xpto (x)
   [[ (x == #x) * (#x == "maria") * types(#x: $$string_type)]]
   [[ (! (x == None)) ]]
   normal
proc xpto (x) {
               skip; 
       rlab:   skip;
       elab:   skip
}
with
{
      ret: 	x, rlab;
      err: 	x, elab;
};
