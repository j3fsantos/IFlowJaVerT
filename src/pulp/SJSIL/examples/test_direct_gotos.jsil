(** Expected result: YES                             **)
spec test0 (y)
   [[ types(y: $$boolean_type) ]]
   [[ ((x, "cenoura") -> #y) * ((x, "@proto") -> $$null) ]]
   normal
proc test0 (y) {
  		   x := new ();
  		   goto [y] then else; 
   then:   [x, "cenoura"] := "maria";
           goto rlab; 
   else:   [x, "cenoura"] := "jose"; 
   rlab:   skip
} with { ret: x, rlab; };


(** Expected result: NO                             **)
spec test1 (y)
   [[ types(y: $$boolean_type) ]]
   [[ ((x, "cenoura") -> "maria") * ((x, "@proto") -> $$null) ]]
   normal
proc test1 (y) {
  		   x := new ();
  		   goto [y] then else; 
   then:   [x, "cenoura"] := "maria";
           goto rlab; 
   else:   [x, "cenoura"] := "jose"; 
   rlab:   skip
} with { ret: x, rlab; };


(** Expected result: YES                             **)
spec test2 (y)
   [[ (y == $$t) * types(y: $$boolean_type) ]]
   [[ ((x, "cenoura") -> "maria") * ((x, "@proto") -> $$null) ]]
   normal
proc test2 (y) {
  		   x := new ();
  		   goto [y] then else; 
   then:   [x, "cenoura"] := "maria";
           goto rlab; 
   else:   [x, "cenoura"] := "jose"; 
   rlab:   skip
} with { ret: x, rlab; };


(** Expected result: NO                             **)
spec test3 (y)
   [[ (y == $$f) * types(y: $$boolean_type) ]]
   [[ ((x, "cenoura") -> "maria") * ((x, "@proto") -> $$null) ]]
   normal
proc test3 (y) {
  		   x := new ();
  		   goto [y] then else; 
   then:   [x, "cenoura"] := "maria";
           goto rlab; 
   else:   [x, "cenoura"] := "jose"; 
   rlab:   skip
} with { ret: x, rlab; };



