pred interval (x, a, b) : 
	(! (x <# a)) * (x <=# b) * (a <=# b) * types(x: $$int_type, a: $$int_type, b: $$int_type);

spec interval_addition (x, a, b)
   [[ interval(x, a, b) ]]
   [[ interval(ret, (a + 1), (b + 1)) ]]
   normal
proc interval_addition (x, a, b) {
			x := x * 1;
   rlab:	skip
} 
with 
{ 
	ret: x, rlab; 
};