import Array, Boolean, Errors, Function, Global, Init, Internals, Math, Number, Object, String;

proc main () {

			xret := "setupInitialHeap" ();
			
			xret := "i__toObject" ($$null) with elab;
			
	rlab:	skip;
	elab:	skip
}
with
{
	ret: 	xret, rlab;
	err:	xret, elab;
}