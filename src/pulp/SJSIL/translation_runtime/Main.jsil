import Init, Internals, String, Errors;

proc main () {
			
			xret := "setupInitialHeap" ();

			xret := "i__sameValue" (-0, -0);

	rlab:	skip;
	elab:	skip
}
with
{
	ret: 	xret, rlab;
	err:	xret, elab;
}