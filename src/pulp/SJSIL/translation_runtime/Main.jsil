(* import Array, Boolean, Errors, Function, Global, Init, Internals, Math, Number, Object, String; *)

import Errors;

proc main () {
	
	rlab:	xret := "i__toObject" ($$null) with elab;
	elab:	skip
}
with
{
	ret: 	xret, rlab;
	err:	xret, elab;
}