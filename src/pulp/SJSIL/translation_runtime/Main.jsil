(* import Array, Boolean, Errors, Function, Global, Init, Internals, Math, Number, Object, String; *)

import Errors;

proc main () {
			xret := 5;
			goto [xret = 5] terr rlab;
			
	rlab:	skip;
	
	terr:	xret := "TypeError" ();
	elab:	skip
}
with
{
	ret: 	xret, rlab;
	err:	xret, elab;
}