(** Totals: 1 fails and 0 pass **)

(** Expected result: NO                             **)
spec separation_nonsense_1 (x, y, z)
	[[ ((x, y) -> "a") * ((x, z) -> "b") ]]
	[[ ((x, y) -> "a") * ((x, z) -> "b") * (ret == "b") ]]
	normal
proc separation_nonsense_1 (x, y, z) {
			xret := [x, y];
	rlab:	skip
} with {
	ret: xret, rlab;
};
