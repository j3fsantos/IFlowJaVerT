(** Totals: 0 fails and 1 pass **)


(** Expected result: YES                             **)
spec test0 (x)
   [[ types(x: $$number_type) ]]
   [[ types(x: $$number_type) * (x <=# 10) ]]
   normal
proc test0 (x) {
		   skip;
   [[ types(x: $$number_type) ]]
   head:   goto [(not (x <= 10))] then1 rlab;
   then1:   x := x - 1;
  		   goto head;
   rlab:   skip

} with { ret: x, rlab; };
