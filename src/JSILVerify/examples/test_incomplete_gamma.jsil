(** Totals: 0 fails and 2 passes **)

(** Tested feature: field assignment 1               **)
(** Expected result: YES                             **)
spec test0 (x)
   [[ (x == #x) * types(x: Num) ]]
   [[ (ret == (#x + 1)) ]]
   normal
proc test0 (x) {
  		   x := x + 1;
   rlab:   skip
} with { ret: x, rlab; };


(**               **)
(** Expected result: YES                             **)
spec test1 (x)
   [[ (x == #x) * types(#x: List) ]]
   [[ (ret == l-len #x) ]]
   normal
proc test1 (x) {
  		   x := l-len (x) ;
   rlab:   skip
} with { ret: x, rlab; };
