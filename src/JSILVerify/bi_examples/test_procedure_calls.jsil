(** Totals: 3 fails and 5 passes **)


(** Expected result: FAILS **)
(** spec test1 (y)
   [[ emp ]]
   [[ emp  ]]
   normal
proc test1 (y) {
  		   x := new();
  		   x := "aux1" (x, y); 
   rlab:   skip
} with { ret: x, rlab; }; *)


(** Expected result: FAILS **)
spec test2 (x, y)
   [[ ((x, "@proto") -> $$null) ]]
   [[ emp  ]]
   normal
proc test2 (x, y) {
  		   x := "aux1" (x, y); 
   rlab:   skip
} with { ret: x, rlab; }; 



(** Expected result: YES                             **)
spec aux1 (x, y)
   [[ ((x, "cenoura") -> #y) * ((x, "@proto") -> $$null) * (! (#y == None)) * ((x, "tutifruti") -> #w) ]]
   [[ ((x, "cenoura") -> "jose") * ((x, "@proto") -> $$null) * ((x, "tutifruti") -> y) ]]
   normal
proc aux1 (x, y) {
           [x, "cenoura"] := "jose";
           [x, "tutifruti"] := y; 
   rlab:   skip
} with { ret: x, rlab; };
